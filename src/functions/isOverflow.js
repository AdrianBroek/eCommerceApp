// import React, {useState, useLayoutEffect} from 'react';

export function useIsOverflow (element, callback) {
    // const [isOverflow, setIsOverflow] = useState(undefined);

    // setIsOverflow(element.scrollHeight > element.clientHeight)

    // return isOverflow;
    console.log('siema')
};